<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@drawable/shape_splash_bg</item>
    </style>

    <!--PopWindow弹出动画 上下弹出-->
    <style name="PopWindowUpStyle">
        <item name="android:windowEnterAnimation">@anim/pop_up_show</item>
        <item name="android:windowExitAnimation">@anim/pop_up_hidden</item>
    </style>

    <style name="PopWindowDownStyle">
        <item name="android:windowEnterAnimation">@anim/pop_down_show</item>
        <item name="android:windowExitAnimation">@anim/pop_down_hidden</item>
    </style>

    <declare-styleable name="MaxHeightRecyclerView">
        <attr name="maxHeight" format="dimension" />
    </declare-styleable>

    <style name="TabLayoutTextStyle">
        <item name="android:textSize">14sp</item>
    </style>

</resources>